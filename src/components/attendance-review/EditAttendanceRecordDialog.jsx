import React, { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";

export default function EditAttendanceRecordDialog({ record, onClose, onSave, saving }) {
  const [status, setStatus] = useState("present"); const [reason, setReason] = useState(""); const [file, setFile] = useState(null);
  useEffect(() => { if (record) { setStatus(record.status === "not_taken" ? "present" : record.status); setReason(record.excused_reason || ""); setFile(null); } }, [record]);
  return <Dialog open={Boolean(record)} onOpenChange={(open) => !open && onClose()}><DialogContent><DialogHeader><DialogTitle>Update attendance</DialogTitle></DialogHeader>{record && <p className="text-sm text-slate-500">{record.student_name} · {record.class_name} · {record.date}</p>}<label className="text-sm font-medium text-slate-700">Status<select value={status} onChange={(event) => setStatus(event.target.value)} className="mt-1.5 h-10 w-full rounded-md border border-input bg-white px-3 text-sm"><option value="present">Present</option><option value="absent">Absent</option><option value="late">Late</option><option value="excused">Excused</option></select></label>{status === "excused" && <><label className="text-sm font-medium text-slate-700">Evidence type<select value={reason} onChange={(event) => setReason(event.target.value)} className="mt-1.5 h-10 w-full rounded-md border border-input bg-white px-3 text-sm"><option value="">Select evidence type…</option><option>Parent Note</option><option>Medical Note</option><option>Professional Note</option></select></label><label className="text-sm font-medium text-slate-700">Evidence file<input type="file" accept="image/*,.pdf" onChange={(event) => setFile(event.target.files?.[0] || null)} className="mt-1.5 block w-full text-sm text-slate-600" /></label></>}<DialogFooter><Button variant="outline" onClick={onClose}>Cancel</Button><Button disabled={saving} onClick={() => onSave({ status, excused_reason: reason, file })}>{saving ? "Saving…" : "Save and lock"}</Button></DialogFooter></DialogContent></Dialog>;
}