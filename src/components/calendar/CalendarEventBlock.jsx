import React from "react";
import { Shield } from "lucide-react";
const styles = { personal: "border-dashed border-indigo-300 bg-indigo-50 text-indigo-700", school_event: "border-teal-500 bg-teal-50 text-teal-800", drill: "border-rose-500 bg-rose-50 text-rose-800" };
export default function CalendarEventBlock({ event, style, onContextMenu }) { const drill = event.visibility === "drill"; const studentCount = event.affected_student_ids?.length || 0; return <button onContextMenu={onContextMenu} className={`absolute rounded-lg border p-1.5 text-left text-[10px] leading-tight overflow-hidden ${styles[event.visibility]}`} style={style} title={`${event.title} · ${event.start_time}–${event.end_time}`}><p className="flex items-center gap-1 font-semibold truncate">{drill && <Shield className="h-3 w-3 shrink-0" />}{event.title}</p><p className="truncate opacity-80">{event.start_time}–{event.end_time}</p>{event.visibility === "school_event" && studentCount > 0 && <span className="mt-1 inline-block rounded bg-teal-200 px-1 text-[9px] font-semibold">{studentCount} students out</span>}</button>; }